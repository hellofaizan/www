---
title: Demo Post 2
description: This is a demo post.
date: 09-05-2024
Author: HelloFaizan
AuthorUrl: 'https://tielinks.vercel.app/hellofaizam'
published: true
featured: false
---

# SearchBar

```tsx {4, 5} showLineNumbers
const copyToClipboard = async () => {
    try {
      // Get the pre element's text content
      const preElement = document.querySelector("pre");
      const codeElement = preElement?.querySelector("code");

      // Get raw text content, removing any extra whitespace
      const codeToCopy = codeElement?.textContent || "";

      await navigator.clipboard.writeText(codeToCopy);
      setIsCopied(true);
      setTimeout(() => setIsCopied(false), 2000);
    } catch (err) {
      console.error("Failed to copy text: ", err);
    }
  };
```

### Link component

[Link Title](https://hellofaizan.com) [discuss ![Cal](/companylogo/calcom.svg)](https://cal.com/hellofaizan/personalmeet)